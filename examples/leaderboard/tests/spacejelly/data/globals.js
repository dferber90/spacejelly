global.timing = {
	pause: 500,
	longPause: 3000,
	timeout: 1000
};

global.paths = { // can be prepended to http://localhost:4096/ in tests
	home: '',
	login: 'login.html'
};

global.selectors = {
	body: 'body',
	leaderboard: '.leaderboard',
	player: '.player'
};